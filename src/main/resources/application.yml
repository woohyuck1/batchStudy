server:
  port: 9090

spring:
  application:
    name: batch-study
  datasource:
    url: jdbc:mariadb://192.168.45.252/batch-study?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 4646
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true
    show-sql: true

jwt:
  secret: YourSuperSecretKeyThatIsAtLeast256BitLong
  expiration: 86400000

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    tags-sorter: alpha
    operations-sorter: alpha
